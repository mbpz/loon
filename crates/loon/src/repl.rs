//! Interactive REPL that streams chat over WebSocket.
//!
//! Filled in by Task 9.2; for now this is a compile-only stub so
//! the CLI skeleton builds.

pub async fn run(_server_url: &str, _session_id: &str) -> anyhow::Result<()> {
    Ok(())
}
